# Build Log — {project-name}

**Repo:** https://github.com/{org}/{repo}.git  
**Platform:** OpenShift / Buildah

---

## Overview

1. {ขั้นตอนที่ 1 และผลลัพธ์}
2. {ขั้นตอนที่ 2 และผลลัพธ์}
3. {ขั้นตอนที่ 3 และผลลัพธ์}
4. {แก้ไข} → build ผ่าน ✅

---

## {YYYY-MM-DD HH:MM:SS UTC} | {base-image} + {tool/version}

**Commit:** `{hash}` — {commit message}

**Error:**
```
{error message ที่สำคัญ}
```

**สาเหตุ:** {อธิบายสั้น ๆ ว่าทำไมถึง fail}

**Full Log:**
```bash
{log output ที่เกี่ยวข้อง}
```

---

## {YYYY-MM-DD HH:MM:SS UTC} | {base-image} + {tool/version}

**Commit:** `{hash}` — {commit message}  
_(หมายเหตุ เช่น retry กี่ครั้ง หรือไม่มี clone section)_

**Error:**
```
{error message ที่สำคัญ}
```

**สาเหตุ:** {อธิบายสั้น ๆ}

**แนวทางแก้ไข:**
- {วิธีที่ 1}
- {วิธีที่ 2}

---

## {YYYY-MM-DD HH:MM:SS UTC} | ✅ Build สำเร็จ

**Commit:** `{hash}` — {commit message}

**สิ่งที่แก้ไข:**
- {รายการที่เปลี่ยน}
- {รายการที่เปลี่ยน}